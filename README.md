# timecodeFixer
Timecode Fixer reads two XML files, extracts clip names and timecodes, matches clips by numeric identifiers, and updates incorrect timecodes in the edit XML using the accurate values from the source XML. The corrected file is exported as output.xml.
